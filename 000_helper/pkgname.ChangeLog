2014-01-22 carstene1ns <url/mail: arch carsten-teibes de>

	* pkgver-pkgrel :
	new upstream release.
	Reason we changed stuff.
	Another reason we changed stuff.

	* gcc-4.1.patch :
	Removed, no longer needed.

2013-12-15 carstene1ns <url/mail: arch carsten-teibes de>

	* 1.0-5 :
	added ChangeLog.
	the last line should end with just one newline.
	you can cat the file to check it displays fine.
